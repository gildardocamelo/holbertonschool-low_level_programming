#include <stdio.h>

/**
 * main - Prints the lowercase alphabet in reverse.
 *
 * Return: Always 0.
 */

int main(void)
{
int letter = 122;

while (letter >= 97; letter ++)
putchar(letter);

putchar('\n');

return (0);
}
